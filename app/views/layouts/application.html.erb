<!DOCTYPE html>
<html>
  <head>
    <title>RailsVentasApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <!-- Agrego un parital(porcion pequeña de codigo html reutilizable) -->
    <%= render 'shared/header' %>
    <%= yield %>
  </body>
</html>
